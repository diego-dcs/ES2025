Operadores Matematicos
    + Adição
    - Subitração
    / Divisão
    * Multiplicação
    % Modulo (Resto de Divisão)
    ** Esponenciação

    -- Decremento
    ++ Incremento

Operadores de Comparação
    < Menor que
    > Maior que
    <= Menor ou igual
    >= Maior ou igual
    == Igual a
    != Diferente de
    === identico
    !== Muinto diferente

Operadores Logico
    && Ambos
    || Um ou ambos
    !  !(true) Retorna o contrario
    ?? Se null recebe "string"
